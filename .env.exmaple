# Database
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USERNAME=root
DATABASE_PASSWORD=password
DATABASE_NAME=token_transactions

# Redis (for Bull queues)
REDIS_HOST=localhost
REDIS_PORT=6379

# Webhooks
WEBHOOK_URLS=http://localhost:3001/webhook,http://localhost:3002/webhook

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100

# Database Connection
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=#
DB_DATABASE=myapp

# SMTP Configuration for Alerts
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=#
SMTP_PASS=#

# Alert Configuration
ALERT_EMAIL_FROM=#
ALERT_EMAIL_TO=#

# Slack Configuration
SLACK_WEBHOOK_URL=#
SLACK_CHANNEL=#

# Webhook Configuration
WEBHOOK_URL=#
WEBHOOK_HEADERS=#

# Backup Configuration
BACKUP_DESTINATION=#
BACKUP_ENCRYPTION_KEY=#

# Monitoring Configuration
METRICS_RETENTION_DAYS=90
LOG_RETENTION_DAYS=30
CAPACITY_WARNING_THRESHOLD=80
CAPACITY_CRITICAL_THRESHOLD=95